import '../../../../biznex.dart';

class MarketPage extends HookConsumerWidget {
  final AppColors theme;
  const MarketPage(this.theme, {super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold();
  }
}
